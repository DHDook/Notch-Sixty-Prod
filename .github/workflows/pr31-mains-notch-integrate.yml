name: PR31 Mains Notch Integration

on:
  push:
    branches:
      - feature/noise-hum-suppression
    paths:
      - '.github/scripts/pr31_mains_notch.py'
      - '.github/workflows/pr31-mains-notch-integrate.yml'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: xcode-27
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/noise-hum-suppression

      - name: Apply PR31 mains-notch foundation
        run: python3 .github/scripts/pr31_mains_notch.py

      - name: Run full test suite
        run: >-
          xcodebuild
          -project NotchSixty.xcodeproj
          -scheme NotchSixty
          -configuration Debug
          -destination 'platform=macOS,arch=arm64'
          CODE_SIGNING_ALLOWED=NO
          ENABLE_TESTABILITY=YES
          test

      - name: Commit PR31 mains-notch foundation and clean helpers
        run: |
          rm -f .github/scripts/pr31_mains_notch.py .github/workflows/pr31-mains-notch-integrate.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Add mains-hum harmonic notch foundation'
          git push origin HEAD:feature/noise-hum-suppression
